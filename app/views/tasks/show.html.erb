<%= link_to "DELETE",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>

<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<%= link_to "Update a task", edit_task_path %>

<%= link_to "Back to tasks", tasks_path %>

